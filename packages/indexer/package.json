{
  "name": "@agentcensus/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentCensus — ERC-8004 registry indexer, ERC-8183 job indexer, liveness prober, census stats",
  "scripts": {
    "build": "tsc",
    "cli": "tsx src/index.ts",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
