# RPC overrides (defaults to publicnode; use NodeReal MegaNode key in prod)
BSC_MAINNET_RPC=
BSC_TESTNET_RPC=

# Web
CENSUS_NETWORK=testnet

# Agents (per-agent, set in systemd units — never commit real values)
AGENT_NETWORK=bsc-testnet
AGENT_PRIVATE_KEY=
SERVICE_PRICE_WEI=10000000000000000
