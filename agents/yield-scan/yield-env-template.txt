# AgentCensus Yield Scanner — copy this file to agents\yield-scan\.env and fill in.
# NEVER commit the real .env.
#
# Use a NEW fresh testnet wallet for this agent (its own ERC-8004 identity),
# funded with ~0.01 tBNB for registration + submit gas.

# --- Wallet (required) — the YIELD agent's own fresh testnet key ---
WALLET_PASSWORD=change-me
PRIVATE_KEY=0x...

# --- Network ---
NETWORK=bsc-testnet
RPC_URL=https://bsc-testnet-rpc.publicnode.com

# --- Public URL of this instance (Caddy routes /erc8183y to port 8007) ---
ERC8183_AGENT_URL=https://agentcensus.xyz/erc8183y

# --- Price: 0 during the demo phase ---
ERC8183_SERVICE_PRICE=0

# --- Server ---
PORT=8007
AGENT_NAME=AgentCensus Yield Scanner
